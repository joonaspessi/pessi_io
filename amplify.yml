version: 1
frontend:
  phases:
    preBuild:
      commands:
       - npm i
    build:
      commands:
        - hugo
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
